import {Meta} from '@storybook/addon-docs/blocks'

<Meta title="Design System/Introduction" />

# Seed Design System

This Storybook isolates the `@shm/ui` components used across the Seed apps. Each story is rendered with mock fixtures
and can be toggled between light and dark themes.
